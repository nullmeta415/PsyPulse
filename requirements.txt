# Python dependencies
# We'll install core ML + NLP + dashboard libraries

# Core
pandas>=2.1
numpy>=1.26
scikit-learn>=1.3

# Data ingestion
snscrape @ git+https://github.com/JustAnotherArchivist/snscrape.git@master
    # For Twitter scraping (no API key needed)
requests    # For news API's / RSS
feedparser  # For RSS news ingestion
beautifulsoup4  # For HTML parsing

# PyTorch (GPU with CUDA 12.6)
#torch>=2.4.0  # Required backend for transformers
#torchvision>=0.19.0
#torchaudio>=2.4.0
# Installed from PyTorch custom index:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126

# NLP / ML (Huggingface models)
transformers>=4.41    # Huggingface transformers (sentiment/emotion models)
sentence-transformers>=2.3   # For model embeddings
nltk    # Classic NLP tools for basic NLP preprocessing (stopwords, tokenization)
langdetect    # For language detection

# Visualization & Dashboard
matplotlib
seaborn
plotly
streamlit   # For interactive dashboard

# Utils
pyyaml  # Config loader for reading config.yaml
tqdm    # For progress bars